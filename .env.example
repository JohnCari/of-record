# Copy to .env.local and fill in. Never commit real values.
AI_GATEWAY_API_KEY=      # Vercel AI Gateway key (drafter: google/gemini-3.8-flash)
TYPESAFE_API_KEY=        # Typesafe Jev (verifier + reranker)
COURTLISTENER_TOKEN=     # https://www.courtlistener.com/help/api/rest/ (free)
INVITE_SECRET=           # openssl rand -hex 24
NEXT_PUBLIC_CONVEX_URL=  # written by `convex dev`
