// "Request attorney input." The framework's own tool: the turn parks durably until the attorney
// answers, whether that takes seconds or days. Re-exported because defaultTools is off.
export { default } from "eve/tools/ask_question";
